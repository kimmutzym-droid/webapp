# Google Apps Script로 더 쉽게 자동화하기 (Render/OAuth 설정 없음)

이 방법은 Render 배포나 Google Cloud Console에서 OAuth 클라이언트를 따로 만드는 과정이 없습니다.
구글 계정 로그인 한 번 + Gemini API 키 하나만 있으면 됩니다.

## 1단계. 새 Apps Script 프로젝트 만들기

1. https://script.google.com 접속 (구글 계정으로 로그인)
2. "새 프로젝트" 클릭
3. 기본으로 열린 `Code.gs` 내용을 전부 지우고, 이 저장소의 `apps-script/blogAutomation.gs` 파일 내용을 그대로 복사해서 붙여넣기
4. 우측 상단에서 프로젝트 이름을 원하는 대로 변경 (예: "블로그 자동화")

## 2단계. Blogger API 서비스 추가 (OAuth 클라이언트 발급 불필요)

1. 왼쪽 메뉴 "서비스" 옆 **+** 버튼 클릭
2. 목록에서 **Blogger API** 선택 → "추가" 클릭
3. 이게 끝입니다. 별도로 Google Cloud Console에서 OAuth 클라이언트나 리디렉션 URI를 만들 필요가 없습니다.

## 3단계. Gemini API 키 발급 (무료)

1. https://aistudio.google.com/apikey 접속 → 로그인 → "Create API key" 클릭
2. 나온 키를 복사

## 4단계. 블로그 ID 확인

1. https://www.blogger.com 접속 → 자동 발행할 블로그 선택
2. 주소창 URL 또는 설정 화면에서 블로그 ID 확인이 어려우면, 아래처럼 임시 확인:
   - Apps Script 편집기에서 새 함수를 하나 만들어 `Logger.log(Blogger.Blogs.getByUrl({url: "본인 블로그 주소"}).id)` 실행 → 실행 로그에 나온 숫자가 블로그 ID
   - (이때도 권한 승인 팝업이 뜨면 본인 계정으로 허용)

## 5단계. 스크립트 속성 설정

1. 왼쪽 메뉴 "프로젝트 설정" (톱니바퀴 아이콘) 클릭
2. 아래로 스크롤 → "스크립트 속성" → "스크립트 속성 추가"
3. 아래 두 개 추가:
   - `GEMINI_API_KEY` = 3단계에서 받은 키
   - `BLOG_ID` = 4단계에서 확인한 블로그 ID

## 6단계. 한 번 수동 실행해서 권한 승인

1. 편집기 상단 함수 선택 드롭다운에서 `runDaily` 선택
2. "실행" 버튼 클릭
3. "권한 검토" 팝업이 뜨면 본인 구글 계정 선택 → "고급" → "안전하지 않음으로 이동" (개인 스크립트라 정상적으로 뜨는 경고입니다) → 허용
4. 실행이 끝나면 하단 "실행 로그"에 발행된 글 주소가 보입니다. Blogger에 실제로 글이 올라왔는지 확인

## 7단계. 매일 자동 실행되도록 트리거 등록

1. 왼쪽 메뉴 "트리거" (시계 아이콘) 클릭
2. 우측 하단 "트리거 추가" 클릭
3. 실행할 함수: `runDaily`
4. 이벤트 소스: "시간 기반 트리거"
5. 매일 원하는 시간대(예: 오전 9시~10시) 선택 → 저장

이제부터 매일 정해진 시간에 자동으로 주제를 정하고, 글을 쓰고, Blogger에 발행합니다.

## 참고

- 글쓰기 지침은 `prompts/blog-writing-prompt.md`를 기반으로 코드 안(`WRITING_GUIDE`)에 포함되어 있습니다. 톤이나 규칙을 바꾸고 싶으면 `apps-script/blogAutomation.gs`의 `WRITING_GUIDE` 부분을 수정하면 됩니다.
- 이미지 자동 삽입은 이 버전에는 없습니다 (설정을 최소화하기 위해 제외). 필요하면 추가 요청하면 됩니다.
- 기존 Render + 대시보드 앱(`SETUP.md`)도 그대로 남아있으니, 더 많은 기능(이미지 자동 생성, 여러 블로그 관리, 대시보드 UI)이 필요해지면 그쪽으로 넘어가도 됩니다.
