# Google Apps Script로 반자동화하기 (구글시트 저장 버전)

매일 자동으로 Gemini가 블로그 글을 써서 **구글시트에 한 줄씩 추가**해줍니다.
Blogger에 올리는 건 시트를 보고 직접 복사-붙여넣기 하면 됩니다 (완전 자동 발행보다 안전하고 설정도 더 간단합니다).

OAuth 클라이언트 발급, Render 배포, Blogger API 권한 설정 전부 필요 없습니다.
필요한 건 **구글 계정 로그인 + Gemini API 키 + 구글시트 1개**뿐입니다.

## 1단계. 결과를 저장할 구글시트 만들기

1. https://sheets.google.com 접속 → 새 스프레드시트 만들기 (빈 시트로 시작, 헤더는 스크립트가 자동으로 채움)
2. 시트 이름은 원하는 대로 (예: "블로그 글 큐")
3. 주소창 URL을 보면 `https://docs.google.com/spreadsheets/d/이부분이ID/edit` 형태입니다. 이 ID를 복사해둠

## 2단계. Apps Script 프로젝트 만들기

1. https://script.google.com 접속 (구글 계정으로 로그인)
2. "새 프로젝트" 클릭
3. 기본으로 열린 `Code.gs` 내용을 전부 지우고, 이 저장소의 `apps-script/blogAutomation.gs` 파일 내용을 그대로 복사해서 붙여넣기
4. 우측 상단에서 프로젝트 이름을 원하는 대로 변경 (예: "블로그 글 자동 생성")

## 3단계. Gemini API 키 발급 (무료)

1. https://aistudio.google.com/apikey 접속 → 로그인 → "Create API key" 클릭
2. 나온 키를 복사

## 4단계. 스크립트 속성 설정

1. 왼쪽 메뉴 "프로젝트 설정" (톱니바퀴 아이콘) 클릭
2. 아래로 스크롤 → "스크립트 속성" → "스크립트 속성 추가"
3. 아래 두 개 추가:
   - `GEMINI_API_KEY` = 3단계에서 받은 키
   - `SHEET_ID` = 1단계에서 복사한 시트 ID

## 5단계. 한 번 수동 실행해서 권한 승인

1. 편집기 상단 함수 선택 드롭다운에서 `runDaily` 선택
2. "실행" 버튼 클릭
3. "권한 검토" 팝업이 뜨면 본인 구글 계정 선택 → "고급" → "안전하지 않음으로 이동" (개인 스크립트라 정상적으로 뜨는 경고입니다) → 허용
4. 실행이 끝나면 1단계에서 만든 시트를 열어보세요. 제목/메타설명/라벨/본문 HTML이 한 줄 추가되어 있어야 합니다

## 6단계. 매일 자동 실행되도록 트리거 등록

1. 왼쪽 메뉴 "트리거" (시계 아이콘) 클릭
2. 우측 하단 "트리거 추가" 클릭
3. 실행할 함수: `runDaily`
4. 이벤트 소스: "시간 기반 트리거"
5. 매일 원하는 시간대(예: 오전 9시~10시) 선택 → 저장

이제부터 매일 정해진 시간에 새 글이 자동으로 시트에 쌓입니다.

## 7단계. 실제 발행은 직접 (반자동)

1. 시트를 열어 새로 추가된 행 확인
2. 본문(HTML) 칸 내용을 복사해서 Blogger 글쓰기 화면 → "HTML 보기" 모드에 붙여넣기
3. 제목, meta_description(검색엔진 설명), 라벨도 그대로 옮겨서 채우기
4. Blogger에서 "게시" 클릭
5. (선택) 시트의 "상태" 칸을 '대기중' → '발행완료'로 직접 바꿔서 발행 여부 관리

## 참고

- 글쓰기 지침은 `prompts/blog-writing-prompt.md`를 기반으로 코드 안(`WRITING_GUIDE`)에 포함되어 있습니다. 톤이나 규칙을 바꾸고 싶으면 `apps-script/blogAutomation.gs`의 `WRITING_GUIDE` 부분을 수정하면 됩니다.
- 중복 방지: 최근 생성한 제목 10개를 시트에서 읽어와 Gemini에게 "이미 쓴 주제 피하라"고 알려줍니다.
- 이미지 자동 삽입은 이 버전에는 없습니다 (설정을 최소화하기 위해 제외).
- 기존 Render + 대시보드 앱(`SETUP.md`)도 그대로 남아있으니, Blogger 자동 발행까지 완전 자동화하고 싶어지면 그쪽으로 넘어가도 됩니다.
